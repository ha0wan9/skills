# User Preferences — toy-weather-cli

> **Local only. Git-ignored. Do not commit.**
> Rendered by `render_user_preferences.py` from `USER.template.md`.

## Selected Preset

`minimal` — lightweight harness, no hooks, no phase-lock.

## Enabled Options

- [x] Show a one-line memory-update summary after each session.
- [x] Confirm before creating a new `agents/*.md` file.
- [ ] Enable phase-lock gates (brainstorm → plan → implement → review → finish).
- [ ] Enable issue-tracker integration (Linear / GitHub / Jira).
- [ ] Enable multi-host manifest generation.

## Style

- Commit messages: imperative mood, ≤72 chars.
- Delivery format: bullet list, user-facing first, then agent-facing diff.
