# fixtures

Scope: minimal synthetic run demonstrating direct extraction from a single SVG wireframe into one card component asset and a valid `manifest.json`.

Version: 1.0

Coverage: one wireframe sketch → one extracted component asset; validates that `validate_asset_pack.py` and `render_contact_sheet.py` both run cleanly against the produced manifest.
